import React from 'react';
import Header from '../organisms/header';
import Hero from '../molecules/hero';

const Home = () => {
    return (
        <div>
            <Hero />
        </div>
    );
};

export default Home;
